[project]
name = "mek4470-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.96.0",
    "flax>=0.12.6",
    "jax>=0.9.2",
    "marimo>=0.21.1",
    "matplotlib>=3.10.8",
    "optax>=0.2.8",
    "pandas>=3.0.3",
    "pydantic-ai-slim>=1.85.1",
    "python-pptx>=1.0.2",
    "tabulate>=0.10.0",
    "tqdm>=4.67.3",
]

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
